{
  "name": "{name}",
  "version": "0.0.1",
  "description": "{description}",
  "author": "{author}",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "asc assembly/index.ts -b build/{name}.wasm --use abort=assembly/index/abort --optimize"
  },
  "license": "ISC",
  "dependencies": {
    "@wapc/as-msgpack": "^0.1.10",
    "wapc-guest-as": "git+https://github.com/wapc/wapc-guest-as#v0.2.1"
  },
  "devDependencies": {
    "assemblyscript": "^0.15.1"
  }
}
